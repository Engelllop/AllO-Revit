<Window x:Class="AllO.UI.Views.OneFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AllO — One Filter"
        Style="{StaticResource AllOWindow}"
        Height="360" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource Deep}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Filter Elements" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Select a parameter, operator and value to find elements."
                   FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Parameter" Foreground="{StaticResource TextSecondary}" FontSize="12"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox Grid.Column="1" Style="{StaticResource DarkCombo}"
                      ItemsSource="{Binding Parameters}"
                      SelectedItem="{Binding SelectedParameter}"
                      ItemContainerStyle="{StaticResource DarkComboItem}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Operator" Foreground="{StaticResource TextSecondary}" FontSize="12"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox Grid.Column="1" Style="{StaticResource DarkCombo}"
                      ItemsSource="{Binding Operators}"
                      SelectedItem="{Binding SelectedOperator}"
                      ItemContainerStyle="{StaticResource DarkComboItem}"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Value" Foreground="{StaticResource TextSecondary}" FontSize="12"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBox Grid.Column="1" Style="{StaticResource SearchBox}"
                     Text="{Binding TargetValue, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,16">
            <RadioButton Content="Current Selection" IsChecked="{Binding UseSelection}" Margin="0,0,16,0"
                         Foreground="{StaticResource TextPrimary}" FontSize="12"/>
            <RadioButton Content="Entire Model" IsChecked="{Binding UseSelection, Converter={StaticResource InverseBool}}"
                         Foreground="{StaticResource TextPrimary}" FontSize="12"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource BtnSecondary}" FontSize="12"
                    Command="{Binding CancelCommand}" Padding="18,6" Margin="0,0,8,0"/>
            <Button Content="Select Matching" Style="{StaticResource BtnPrimary}" FontSize="12"
                    Command="{Binding ExecuteCommand}" Padding="18,6"/>
        </StackPanel>
    </Grid>
</Window>
